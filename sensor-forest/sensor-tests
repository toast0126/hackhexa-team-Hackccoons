bool luz;
bool movimiento;
bool peligro;


if (luz == false && movimiento == true) {
    peligro = true;
} else if (luz == true && movimiento == false) {
    peligro = false;
} else {
    peligro = false; // por defecto seguro
}

if (peligro == true) {
    print("SOSPECHOSO");
} else {
    print("SEGURO");
}